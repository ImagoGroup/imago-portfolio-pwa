Imago Group Portfolio PWA - Option A
- Modern layout with square images, never cropped.
- Navbar includes logo (top-left), Home link, and Categories button (opens sidebar).
- Header title 'Imago Group Portfolio' uses Poppins.
- Books page populated with 10 real products (detailed specs + 5-image carousel placeholders).
- Other categories have the same template and 10 placeholder products each (easy to edit).
- PWA: manifest.json, service-worker.js, automatic install prompt implemented in app.js.
